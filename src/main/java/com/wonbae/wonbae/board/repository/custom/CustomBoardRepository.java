package com.wonbae.wonbae.board.repository.custom;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.wonbae.wonbae.board.dto.BoardDTO;
import com.wonbae.wonbae.board.entity.Board;

public interface CustomBoardRepository {
    // Page<BoardDTO> getSearchList(String searchOption, String keyword, Pageable pageable);
    // Board getPrevBoardInfo(Integer boardIdx);
    // Board getNextBoardInfo(Integer boardIdx);
}
